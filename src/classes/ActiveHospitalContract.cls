/**
 * Created by hubert.bogusz on 12.04.2022.
 */

public with sharing class ActiveHospitalContract {
    public static void execute(){

        if (Trigger.isInsert) {
            // code to insert
            }
        else if (Trigger.isUpdate) {
            //code to update
            }
        }
}